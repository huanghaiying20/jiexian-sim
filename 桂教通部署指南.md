# 电路接线模拟 · 桂教通部署指南

> 把现有这套接线模拟仿真页挂到桂教通上，分两步：先**部署仿真页**到外网拿到 HTTPS 地址，再**在桂教通里建一个 Vue 入口页**跳转过去。

## 整体架构

```
┌─────────────────────┐         ┌──────────────────────────┐
│  桂教通 Vue 入口页   │  跳转    │   仿真页（外网 HTTPS）     │
│  （几十行代码）       │ ──────► │   index.html + script.js  │
│  部署在桂教通上       │         │   + style.css + 9张PNG   │
│                     │         │   + 1段MP3  共 7.1 MB     │
└─────────────────────┘         └──────────────────────────┘
```

为什么要分两步：桂教通这套低代码编辑器是给**简单页面**用的，硬塞 1500 行 canvas 画线代码会非常拥挤，而且平台对第三方库、复杂状态管理、`<canvas>` 高级 API 都有拦截。不如把仿真页原样部署到支持静态站点的云服务上，体验更好、维护更省事。

---

## 第一步：部署仿真页到外网

下面给你 3 个方案，按推荐顺序排列，挑一个就行。

### 方案 A：腾讯 EdgeOne Pages（**最推荐**）

- 免费、HTTPS、自动 CDN 加速、和腾讯生态打通
- 静态站点支持，无需备案
- 操作步骤：

1. 打开 https://edgeone.ai/pages 注册/登录（用你的腾讯云账号）
2. 新建项目 → 类型选「静态站点」→ 关联 Git 仓库 或 **直接上传文件夹**
3. 把 `jiexian_sim_桂教通部署包.zip` 解压后，整个目录上传
4. 构建命令留空、输出目录填 `/`（根目录）
5. 部署完成会给你一个形如 `https://xxx.edgeone.app` 的地址

### 方案 B：腾讯云 CloudBase 静态托管

- 同样免费、有 HTTPS
- 但需要先开 CloudBase 环境（多一步）
- 操作步骤：

1. 打开 https://console.cloud.tencent.com/tcb 开通环境
2. 进入「静态网站托管」→ 上传文件夹
3. 把 `jiexian_sim_桂教通部署包.zip` 解压后整个目录上传
4. 拿到形如 `https://xxx-1234567890.tcloudbaseapp.com` 的地址

### 方案 C：Vercel / Netlify（海外服务，但免费好用）

- 适合翻墙方便的老师
- 操作步骤：

1. 打开 https://vercel.com 注册
2. 「Add New Project」→ 选「Import Folder」或关联 GitHub
3. 把 `jiexian_sim_桂教通部署包.zip` 解压后整个目录拖入
4. 一键部署，拿到形如 `https://jiexian-sim.vercel.app` 的地址

### 方案 D：自己学校/老师的服务器

- 用 nginx / apache 把解压后的目录指向一个站点
- 配 SSL 证书（Let's Encrypt 免费）
- 把 `server.js` 丢掉，用 nginx 代替即可

---

## 第二步：在桂教通新建 Vue 入口页

部署完仿真页会得到一个 HTTPS 地址，假设是 `https://jiexian-sim.edgeone.app/index.html`。

1. 登录桂教通 → 进入「自定义页面」分组
2. 新建 → 类型选 **Vue 页面** → 名称填「电路接线模拟」
3. 把 `桂教通-Vue入口页代码.txt` 里的整段代码**完整复制**到编辑器
4. **关键修改**：把代码里的
   ```js
   const simulatorUrl = ref('https://YOUR-DEPLOYED-URL/index.html');
   ```
   改成你的实际地址，比如：
   ```js
   const simulatorUrl = ref('https://jiexian-sim.edgeone.app/index.html');
   ```
5. 保存 → 启用
6. 在桂教通「Vue 页面」菜单下就能看到这个入口，点击「开始接线练习」按钮即跳转到仿真页（新窗口打开）

---

## 第三步：试一下

1. 打开桂教通 → 进入「电路接线模拟」入口页
2. 点击「开始接线练习」→ 弹出新窗口
3. 仿真页里默认有 6 个预置元件（漏电保护开关、开关电源、继电器、传感器、蜂鸣器、接线端子排）和 2 根预置连线（电源到变压器的 L/N）
4. 试着拖拽元件、点击端口画线、点提交看分数和烟花效果

---

## 常见问题

**Q1：为什么不用 iframe 嵌入桂教通？**
A：仿真页有 HTML5 canvas、右键菜单、HTML5 audio，iframe 沙箱大概率被拦截，体验很差。新窗口跳转最稳。

**Q2：仿真页要不要改任何代码？**
A：**不用改一行**。`index.html / script.js / style.css` 原样部署就行。

**Q3：mp3 音效在某些浏览器需要用户先点击一次才能播放？**
A：是的，浏览器自动播放策略限制。仿真页里首次进入需要点任意元件/按钮激活音频上下文，这是 Web 规范，不是 bug。

**Q4：学生访问会不会很慢？**
A：EdgeOne Pages 和 CloudBase 都有 CDN 加速，广西本地访问延迟 < 50ms。

**Q5：仿真页以后要改怎么同步？**
A：改完本地代码，重新打包 zip，覆盖上传到云服务即可。桂教通入口页不用动。

---

## 文件清单

| 文件 | 用途 |
|---|---|
| `jiexian_sim_桂教通部署包.zip` | 仿真页完整包，直接上传到云服务 |
| `桂教通-Vue入口页代码.txt` | 入口页完整 Vue 代码，复制到桂教通 |
| `桂教通部署指南.md` | 本文档 |
| `index.html / script.js / style.css / *.png / *.mp3` | 仿真页源码，zip 内已包含 |
| `server.js` | 本地开发用的 Node 静态服务器，**部署到云服务时不需要** |
